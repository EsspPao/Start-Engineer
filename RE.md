请你基于当前这个 Windows 应用项目，生成一份完整的 Markdown 项目优化分析文档，文件名为：

PROJECT_OPTIMIZATION_REVIEW.md

当前项目状态：

- 项目已经可以正常启动；
- 主要功能已经可以正确运行；
- 已经可以正确打包成 Windows `.exe`；
- 现在的目标不是修复启动或打包问题，而是让这个程序变得更轻量、更稳定、更易用、更像真正的 Windows 桌面应用；
- 希望尽量减少用户操作，提高自动化和人性化体验。

这份文档不是普通用户说明书，而是给另一个 AI / 开发者继续分析和优化项目用的。请基于当前真实代码生成，不要编造不存在的功能。

请包含以下内容：

# 1. 项目当前状态概述

请说明：

- 项目名称；
- 项目用途；
- 当前已经实现的核心功能；
- 当前运行状态；
- 当前打包方式；
- 当前 `.exe` 的大致使用方式；
- 当前项目距离“正式可发布版本”还差哪些方面。

# 2. 技术栈与架构说明

请列出当前项目真实使用的技术栈，包括：

- 前端框架；
- 桌面端框架，例如 Electron / Tauri / 其他；
- 构建工具；
- 打包工具；
- 状态管理方式；
- 样式方案；
- 系统 API 调用方式；
- 本地数据存储方式；
- 资源监控实现方式；
- 程序启动实现方式。

如果某些技术没有使用，请明确写“当前未使用”。

# 3. 项目目录结构说明

请列出当前项目主要目录结构，并解释重要文件作用。

重点说明：

- 应用入口；
- 主进程相关文件；
- 渲染进程相关文件；
- 页面组件；
- 样式文件；
- 应用数据配置；
- 资源监控逻辑；
- 启动程序逻辑；
- 打包配置文件；
- 图标、资源文件。

请用类似下面的格式：

```txt
project-root/
  src/
  electron/
  public/
  package.json
  vite.config.ts
  ...
```

# 4. 当前功能实现分析

请详细分析当前已经实现的功能。

包括但不限于：

## 4.1 应用启动管理

请说明：

- 应用列表是如何定义的；
- 每个应用的路径如何配置；
- 点击启动时调用了什么方法；
- 是否支持自定义路径；
- 是否支持路径不存在时提示；
- 是否支持启动前检测程序是否已经运行；
- 是否支持批量启动；
- 是否支持一键启动常用应用；
- 是否支持启动后自动最小化或隐藏。

## 4.2 分组管理

请说明：

- 当前是否支持应用分组；
- 分组数据结构是什么；
- 分组是否可以新增、修改、删除；
- 分组是否持久化保存；
- 当前分组交互是否方便。

## 4.3 搜索功能

请说明：

- 当前搜索逻辑在哪里；
- 搜索支持哪些字段；
- 是否支持模糊搜索；
- 搜索体验是否自然；
- 是否可以进一步优化。

## 4.4 资源占用监控

请说明：

- 当前是否监控 CPU；
- 当前是否监控内存；
- 是否只监控已配置应用，还是监控所有进程；
- 数据刷新频率是多少；
- 资源数据来源是什么；
- 当前实现是否准确；
- 是否可能造成性能浪费；
- 是否有更轻量的实现方式。

## 4.5 Windows 桌面体验

请说明当前是否支持：

- 开机自启；
- 最小化到托盘；
- 关闭按钮最小化；
- 后台常驻；
- 托盘菜单；
- 快捷键；
- 自动识别已安装程序；
- 自动保存用户配置；
- 窗口大小和位置记忆；
- 深色 / 浅色主题；
- Windows 通知。

如果没有支持，请明确写“当前未支持”。

# 5. 轻量化分析

请重点分析这个程序是否足够轻量。

请从以下角度分析：

- 打包后的 `.exe` 或安装包体积是否偏大；
- Electron / Tauri / 其他框架本身带来的体积影响；
- 当前依赖是否过多；
- 是否存在未使用的依赖；
- 是否可以移除不必要的库；
- 是否存在重复资源；
- 是否存在过大的图片、图标、字体或静态资源；
- 是否可以按需加载；
- 是否可以减少后台轮询；
- 是否可以降低资源监控刷新频率；
- 是否有不必要的日志输出；
- 是否可以减少启动时加载内容。

请列出具体可优化点，并尽量指出涉及文件。

# 6. 用户操作简化分析

请重点分析如何减少用户操作。

请检查当前项目是否可以优化这些体验：

- 首次启动时是否需要用户手动配置很多路径；
- 是否可以自动扫描常见软件安装路径；
- 是否可以自动识别 Steam、微信、浏览器、常见游戏启动器；
- 是否可以记住用户上次选择；
- 是否可以提供“常用应用一键启动”；
- 是否可以提供“工作模式 / 游戏模式 / 日常模式”；
- 是否可以支持拖拽添加应用；
- 是否可以支持从开始菜单导入应用；
- 是否可以支持从桌面快捷方式导入应用；
- 是否可以减少弹窗；
- 是否可以让错误提示更清楚；
- 是否可以提供默认推荐分组；
- 是否可以自动隐藏已失效路径；
- 是否可以自动检测程序是否已经在运行，避免重复启动。

请给出具体优化建议。

# 7. 代码质量分析

请从以下方面分析当前代码质量：

- 组件拆分是否合理；
- 主进程和渲染进程职责是否清晰；
- 是否存在过大的组件文件；
- 是否存在重复代码；
- 是否存在硬编码路径；
- 是否存在魔法数字；
- 类型定义是否完整；
- 错误处理是否完善；
- 日志是否合理；
- 是否方便后续增加功能；
- 是否方便维护应用配置；
- 是否适合继续扩展为正式产品。

请尽量指出具体文件和问题位置。

# 8. 数据存储与配置分析

请说明当前项目如何保存用户配置。

重点分析：

- 应用列表保存在哪里；
- 用户自定义路径保存在哪里；
- 分组配置保存在哪里；
- 窗口设置是否保存；
- 是否使用 localStorage；
- 是否使用 JSON 文件；
- 是否使用数据库；
- 是否使用 Electron Store 或类似方案；
- 配置文件是否容易迁移；
- 配置损坏时是否有恢复机制；
- 用户卸载或升级后配置是否会丢失。

# 9. 错误处理与稳定性分析

请检查当前项目是否处理了以下情况：

- 应用路径不存在；
- 应用启动失败；
- 权限不足；
- 程序已经运行；
- 资源监控失败；
- 配置文件读取失败；
- 配置文件格式错误；
- Windows API 调用失败；
- 打包后路径变化；
- 用户删除了某个应用；
- 杀毒软件拦截；
- 中文路径或空格路径；
- 非管理员权限运行。

请说明当前处理情况和改进建议。

# 10. UI / 交互体验分析

请分析当前界面是否足够好用。

重点看：

- 主界面是否清晰；
- 是否像一个真正的 Windows 桌面应用；
- 应用卡片信息是否够用；
- CPU / 内存数据显示是否直观；
- 搜索框位置是否合理；
- 分组导航是否清楚；
- 按钮文案是否自然；
- 空状态是否友好；
- 错误状态是否友好；
- 是否需要新手引导；
- 是否需要设置页面；
- 是否需要更好的应用图标；
- 是否需要托盘图标；
- 是否支持窗口缩放；
- 小屏幕下是否正常显示。

# 11. 打包后体验分析

虽然当前项目已经可以正确打包成 `.exe`，但请继续分析：

- 打包后的启动速度；
- 打包后的体积；
- 是否需要安装包；
- 是否需要绿色版；
- 是否需要自动更新；
- 是否需要签名证书；
- 是否会被 Windows Defender 提示；
- 图标是否正确；
- 应用名称是否正确；
- 任务栏显示是否正确；
- 卸载是否干净；
- 用户数据是否保留。

# 12. 安全性分析

请分析当前项目是否存在安全风险：

- 是否允许启动任意路径；
- 是否可能执行危险命令；
- 是否暴露 Node.js 能力给前端；
- Electron preload 是否安全；
- IPC 通信是否做了限制；
- 是否有命令注入风险；
- 是否有路径注入风险；
- 是否有不必要的系统权限；
- 是否有外部网络请求；
- 是否有隐私数据收集。

如果当前项目没有相关风险，也请说明原因。

# 13. 优化建议清单

请按照优先级给出具体、可执行的优化建议。

## 高优先级

主要关注：

- 减少用户操作；
- 提高稳定性；
- 避免启动失败；
- 避免路径配置麻烦；
- 避免重复启动；
- 降低资源监控开销；
- 完善错误提示；
- 保存用户配置。

## 中优先级

主要关注：

- 代码结构优化；
- 配置管理优化；
- UI 体验优化；
- 托盘功能；
- 模式启动；
- 自动扫描应用；
- 设置页面。

## 低优先级

主要关注：

- 动画；
- 视觉美化；
- 主题切换；
- 图标优化；
- 关于页面；
- 版本号展示；
- 自动更新。

每条建议请包含：

- 问题描述；
- 为什么要改；
- 建议怎么改；
- 涉及哪些文件；
- 改动难度：低 / 中 / 高；
- 优先级：高 / 中 / 低。

# 14. 建议的后续开发路线图

请给出一个适合当前项目的后续开发路线图。

请分为：

## 第一阶段：让程序更好用

目标：减少用户操作，提高基础体验。

## 第二阶段：让程序更稳定

目标：完善错误处理、配置存储、资源监控。

## 第三阶段：让程序更像正式产品

目标：托盘、开机自启、设置页、自动更新、图标、安装包体验。

## 第四阶段：进一步轻量化和长期维护

目标：减少依赖、优化性能、整理代码结构。

# 15. 需要人工确认的问题

请列出无法从代码中判断、需要我确认的问题，例如：

- 这个程序最终是自己用，还是准备分享给别人用；
- 是否必须保持绿色版；
- 是否需要安装包；
- 是否需要开机自启；
- 是否希望常驻后台；
- 是否希望关闭窗口时不退出；
- 是否需要自动扫描电脑上的应用；
- 是否需要管理游戏账号或启动参数；
- 是否需要支持多个用户配置；
- 是否需要联网更新；
- 是否需要美化成游戏启动器风格，还是保持 Windows 工具风格。

# 16. 附录：关键文件摘要

请列出当前项目最关键的文件，并说明每个文件的作用。

例如：

- package.json
- 主进程入口文件
- preload 文件
- 渲染进程入口文件
- 主界面组件
- 应用列表配置文件
- 资源监控相关文件
- 启动程序相关文件
- 打包配置文件
- 样式文件

要求：

1. 必须基于当前真实代码；
2. 不要编造不存在的功能；
3. 如果无法判断，请写“当前代码中未发现”；
4. 尽量引用具体文件路径；
5. 重点分析如何让程序更轻量、更稳定、更少用户操作；
6. 生成完成后，请告诉我 `PROJECT_OPTIMIZATION_REVIEW.md` 保存在哪个路径。