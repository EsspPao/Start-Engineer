# 安全策略

## 支持范围

公开预览阶段仅维护最新发布版本。安全修复可能直接进入下一个补丁版本。

## 报告漏洞

请不要在公开 Issue 中披露可利用细节、用户本地路径、配置文件或诊断数据。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。若该功能尚未启用，请先通过仓库所有者的 GitHub 主页联系维护者，并只提供最小必要信息。

建议包含：

- 受影响版本与 Windows 版本；
- 可复现步骤和实际影响；
- 是否涉及任意命令执行、权限提升、下载校验绕过或本地数据泄露；
- 可安全分享的日志或最小示例。

维护者确认问题前，请勿公开利用代码。

## 安全边界

- 主界面默认保持普通权限；高权限 helper 只接受受约束的进程终止协议，不接受任意命令或路径。
- Everything/ES 下载必须通过固定 SHA-256 校验。
- Start Engineer 会启动与结束用户配置的程序。请只添加可信可执行文件，并在结束进程前保存工作。
